bool isPalindrome(int x) {

    int invertido = 0;
    int original = x;    
    
    while (x > 0){
        invertido = invertido * 10 + (x % 10);
        x = x / 10;
    }
    return invertido == original;

}
